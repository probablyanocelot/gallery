<Screen>:
    on_leave: app.previous_screen = self.name

<ScreenManagement>:
    HomeScreen:
        name: 'home'
    # InputScreen:
    #     name: 'input'
    # SettingScreen:
    #     name: 'settings'
    # DeleteSheet:
    #     name: 'delete_sheet'
    # PickMenu:
    #     name: 'pick_menu'


<HomeScreen>
    BoxLayout:
        orientation: 'horizontal'
        Button:
            font_size: '15dp'
            text: 'x'
            pos_hint: {'center_x': .5}
            size_hint: .10, .10
            on_press: print('pressed!')# root.manager.current = 'pick_menu'