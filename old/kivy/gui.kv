<Screen>:
    on_leave: app.previous_screen = self.name

<ScreenManagement>:
    HomeScreen:
        name: 'home'
    # InputScreen:
    #     name: 'input'
    # SettingScreen:
    #     name: 'settings'
    # DeleteSheet:
    #     name: 'delete_sheet'
    # PickMenu:
    #     name: 'pick_menu'


<HomeScreen>

    BoxLayout:
        orientation: 'horizontal'

        Button:
            font_size: '45dp'
            text: 'GALLERY'
            pos_hint: {'center_x': .5, 'top':1}
            # pos_hint: {'center_x': .5}
            size_hint: .10, .10
            on_press: print('pressed!')# root.manager.current = 'pick_menu'
        Button:
            font_size: '45dp'
            text: 'TYPES'
            pos_hint: {'center_x': .5, 'top':1}
            # pos_hint: {'center_x': .5}
            size_hint: .10, .10
            on_press: print('pressed!')# root.manager.current = 'pick_menu'
        Button:
            font_size: '45dp'
            text: 'FORMAT'
            pos_hint: {'center_x': .5, 'top':1}
            size_hint: .10, .10
            on_press: print('pressed!')# root.manager.current = 'pick_menu'
        Button:
            font_size: '45dp'
            text: 'FAVORITES'
            pos_hint: {'center_x': .5, 'top':1}
            # pos_hint: {'center_x': .5}
            size_hint: .10, .10
            on_press: print('pressed!')# root.manager.current = 'pick_menu'
    
    BoxLayout:
        orientation: 'horizontal'
        Label:
            # canvas.before:
                # Color:
                    # rgba: 1,0,0,1
                # Rectangle:
                    # pos: self.pos
                    # size: self.size
            font_size: '45dp'
            text: '0/100 left'
            pos_hint: {'top':0.9, 'center_x': 0.1,}  
            size_hint: .01, .10
        Label:
            # canvas.before:
                # Color:
                    # rgba: 1,0,0,1
                # Rectangle:
                    # pos: self.pos
                    # size: self.size
            font_size: '45dp'
            text: '0/500 fav'
            pos_hint: {'top':0.9, 'center_x': 0.1,}  
            size_hint: .01, .10
    BoxLayout:
        orientation: 'horizontal'
        ImageGallery: